import React from 'react';
import RotatingSpinner from '.';

export default {
  title: 'Atoms/Spinner',
  component: RotatingSpinner,
};

const Template = () => <RotatingSpinner />;

export const Default = Template.bind({});
